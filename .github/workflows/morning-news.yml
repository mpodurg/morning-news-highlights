name: Morning News

on:
  schedule:
    - cron: "0 13 * * *"
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send morning news
        run: curl -fsS -X POST "${{ secrets.MORNING_NEWS_URL }}/api/send"
